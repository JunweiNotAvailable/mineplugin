import React from 'react'
import { AppProps } from '../../utils/Interfaces'

const EditProfile: React.FC<AppProps> = ({ user, setUser }) => {
  return (
    <div>
      {/* picture */}
      {/* username */}
      {/* nickname */}
    </div>
  )
}

export default EditProfile